{{ block.shopify_attributes }}
{% liquid
  for block in block.blocks
    render block
  endfor
%}

{% schema %}
{
  "name": "Group",
  "class": "group",

  "settings": [],
  "blocks": [{ "type": "@app" }, { "type": "@theme" }],
  "presets": [
    {
      "name": "Group",
      "category": "Elements"
    }
  ]
}
{% endschema %}
