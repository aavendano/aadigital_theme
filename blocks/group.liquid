{% liquid
  for block in block.blocks
    render block
  endfor
%}

{% schema %}
{
  "name": "Group",
  "tag": "div",
  "class": "a:group",
  "settings": [],
  "blocks": [{ "type": "@app" }, { "type": "@theme" }],
  "presets": [
    {
      "name": "Group",
      "category": "Elements"
    }
  ]
}
{% endschema %}
