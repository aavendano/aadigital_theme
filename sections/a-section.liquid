{{ section.shopify_attributes }}
{% liquid
  for block in section.blocks
    render block
  endfor
%}

{% schema %}
{
  "name": "Section",
  "class": "a:section",

  "settings": [],
  "blocks": [{ "type": "@app" }, { "type": "@theme" }],
  "presets": [
    {
      "name": "Section",
      "category": "Elements"
    }
  ]
}
{% endschema %}
